<nav role="navigation" aria-label="Fil d'ariane">
    <ol class="flex gap-1">
        {% for breadcrumb_item in breadcrumb %}
            <li class="inert:opacity-40 text-gray-400 {{ "truncate overflow-hidden" if loop.last else "" }}" {{ "inert" if loop.last else "" }}>
                <a 
                    class="px-2 py-1 rounded-md hocus:bg-gray-200 hocus:text-gray-900 text-sm" href="/{{ breadcrumb_item }}"
                >
                    {{ breadcrumb_item.split('/') | last | capitalize }}
                </a>
            </li>
            {% if loop.last == false %}
                <li inert class="text-gray-400 last:hidden">❯</li>
            {% endif %}
        {% endfor %}
    </ol>
</nav>